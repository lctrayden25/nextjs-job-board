/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      { hostname: "mvg7f1vqz17sxxtm.public.blob.vercel-storage.com" },
    ],
  },
};

export default nextConfig;
